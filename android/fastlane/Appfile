json_key_file "" # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name "com.labhouse.rankai" # e.g. com.krausefx.app

for_platform :android do
    for_lane :submit_android_dev_app do
        package_name 'com.labhouse.rankai.dev'
    end
end

for_platform :android do
    for_lane :submit_android_preprod_app do
        package_name 'com.labhouse.rankai.preprod'
    end
end